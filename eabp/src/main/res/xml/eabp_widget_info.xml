<?xml version="1.0" encoding="utf-8"?>
<!-- targetCell*, previewLayout, and description are read on API 31+ and
     ignored below; min* / resizeMode carry the pre-31 launchers. Updates are
     pushed from Emacs via the surface cache, hence updatePeriodMillis=0. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_eabp_agenda"
    android:previewLayout="@layout/widget_eabp_agenda"
    android:description="@string/widget_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />